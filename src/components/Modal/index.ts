import Root from './Root';
import Button from './Button';
import Popup from './Popup';

import Composition from '../../pages/Upload/components/Modal/Composition';

export const Modal = {
  Root,
  Button,
  Popup,
};

export default Composition;
